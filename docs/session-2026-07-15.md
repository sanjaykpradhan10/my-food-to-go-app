# Session — 2026-07-15

**Tool:** Claude Code
**Duration:** Ch. 3 REST + circuit breaker implementation (full brainstorm → spec → plan → subagent-driven build → PR → merge cycle); moved on to Ch. 4
**Repo:** https://github.com/sanjaykpradhan10/my-food-to-go-app
**PR:** https://github.com/sanjaykpradhan10/my-food-to-go-app/pull/1 (merged)

---

## What we did

### 1. Finished Ch. 3 reading, chose to implement the synchronous RPI + circuit breaker pattern first

Ch. 3 covers several IPC patterns (sync REST/RPC, async messaging, circuit breaker, service discovery, transactional outbox, transaction log tailing). Decided to implement just **synchronous REST + circuit breaker** (`order-service` → `restaurant-service`) in this pass, deferring the rest (messaging, transactional outbox, transaction log tailing, service discovery) to a later session.

### 2. Brainstormed and wrote a design spec

`docs/superpowers/specs/2026-07-15-ch3-rest-circuit-breaker-design.md` — key decisions:
- Spring `RestClient` (not OpenFeign or WebClient-blocking) for the HTTP call
- Resilience4j `@CircuitBreaker` annotation approach, bundled into the same pass as the REST call (not deferred)
- `Order` stays **in-memory only** for this pass — persistence deferred to Ch. 4 sagas
- `restaurant-service` gets a minimal persisted `Restaurant`/`MenuItem` model + seed data

### 3. Wrote a 6-task implementation plan

`docs/superpowers/plans/2026-07-15-ch3-rest-circuit-breaker.md` — full task-by-task plan with complete code for every file.

### 4. Executed via subagent-driven development

Isolated worktree (`worktree-ch3-rest-circuit-breaker`, via `EnterWorktree`). Each of the 6 tasks: fresh implementer subagent → task-scoped reviewer subagent (spec + quality) → fix loop if needed. All 6 tasks approved with only Minor findings. Notable judgment calls caught and verified during review:
- Task 3 added a test-only `TestConfig` (mock `RestaurantServicePort` bean) so `contextLoads` would pass before the real adapter existed in Task 4 — later found redundant and removed (see below).
- Task 4 needed two mechanical fixes: corrected `ClientHttpRequestFactoryBuilder`/`Settings` import paths for the actual resolved Spring Boot 3.5.3, and changed `@DirtiesContext` to `AFTER_EACH_TEST_METHOD` to stop the singleton `CircuitBreakerRegistry` from leaking open-breaker state across WireMock test methods.
- Task 6 (manual e2e verification) had to swap "kill the process" for a stalled-TCP-listener stand-in, since a closed port fails instantly and doesn't exercise the RestClient's 2s read timeout the circuit breaker relies on.

### 5. Final whole-branch review (Opus) + fix pass

Ready to merge: Yes, no Critical/Important issues. 4 Minor findings; fixed 2 of them in a single follow-up commit:
- Removed the now-redundant `TestConfig` (Task 4's real adapter makes it unnecessary)
- Added controller-level validation so a malformed `POST /orders` (missing `restaurantId`, empty `lineItems`) returns `400` instead of a misleading `503` (previously fell through the circuit-breaker fallback)

Deferred (not fixed, noted for later): no test locking `RestaurantResponse`/`RestaurantInfo` JSON shapes together across services; `order-service` still requires MySQL to boot despite `Order` being entirely in-memory (inherited from the original stub scaffold).

### 6. Pushed, PR'd, merged

PR #1 merged to `main` via `gh pr merge --merge`. Worktree and remote branch cleaned up. Local `main` fast-forwarded.

### 7. Updated CONTEXT.md and moved to Ch. 4

Ch. 3 marked "Implementing" (partial — only RPI + circuit breaker built). Ch. 4 ("Managing transactions with sagas") marked "Reading" — user will read it independently before the next session.

---

## What's implemented now

- **restaurant-service** (port 8085): `Restaurant`/`MenuItem` JPA entities, seed data (2 restaurants), `GET /restaurants/{id}`
- **order-service** (port 8082): in-memory `Order` domain, `RestaurantServicePort` (hexagonal outbound port), `OrderService` (validates menu items against the restaurant), `RestaurantServiceProxy` (RestClient + Resilience4j `@CircuitBreaker`, instance `restaurantService`: sliding-window-size 5, failure-rate-threshold 50%, wait-duration-in-open-state 5s, 2s connect/read timeout, `RestaurantNotFoundException` ignored so 404s don't trip the breaker), `POST /orders` (201/400/404/503)
- Verified end-to-end manually: happy path, circuit breaker opening under sustained failure, recovery after wait duration

## What's still open in Ch. 3 (not built)

- Async messaging (Kafka / Eventuate Tram)
- Transactional outbox pattern
- Transaction log tailing (CDC via Debezium)
- Service discovery (client-side / server-side)

---

## Resuming in a new session

### In Claude Code
Open the project and say:
> "Read CONTEXT.md and docs/session-2026-07-15.md. I've finished reading Ch. 4 — let's brainstorm the saga implementation."

(Or, if not done reading yet: "I'm still reading Ch. 4, just catching you up.")

### In Claude Chat
Paste `CONTEXT.md` and this file, then say:
> "I'm working through Microservices Patterns Ch. 4. Here is my context and last session notes. Resume from where I left off."

---

## Project state at end of session

```
Git log (main, HEAD):
1214137 docs: move study tracker to Ch.4 (sagas) reading
d6f32fd Merge pull request #1 from sanjaykpradhan10/worktree-ch3-rest-circuit-breaker
1a5e517 fix(order-service): remove redundant TestConfig, validate order request before proxying to restaurant-service
8b1fbb1 docs: update CONTEXT.md — Ch.3 REST + circuit breaker pattern implemented
888a3e7 feat(order-service): add POST /orders endpoint with 404/503 error handling
7e6bd1e feat(order-service): add RestaurantServiceProxy with RestClient and Resilience4j circuit breaker
48f539c feat(order-service): add Order domain model, RestaurantServicePort, and OrderService
2f9c6e9 feat(restaurant-service): add GET /restaurants/{id} endpoint
b612f5e feat(restaurant-service): add Restaurant/MenuItem domain model, repository, seed data
e1a42f6 docs: add implementation plan for Ch.3 REST + circuit breaker pattern
a7e54d1 docs: add design spec for Ch.3 REST + circuit breaker (order->restaurant)
```

All commits pushed to `main`. PR #1 merged and closed. No open branches, no worktrees.

## Next actions
- [ ] Read Ch. 4 — Managing transactions with sagas
- [ ] Come back and brainstorm the saga implementation (likely: `Create Order` saga across order/kitchen/restaurant/accounting/delivery services — choreography vs orchestration)
- [ ] Eventually circle back to finish Ch. 3's remaining patterns (messaging, transactional outbox, transaction log tailing, service discovery) — not urgent, sagas will likely need the messaging infra anyway
