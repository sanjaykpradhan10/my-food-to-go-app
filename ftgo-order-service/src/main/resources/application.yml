spring:
  application:
    name: ftgo-order-service
  datasource:
    url: jdbc:mysql://localhost:3306/ftgo_order
    username: ftgo
    password: ftgo
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8082
